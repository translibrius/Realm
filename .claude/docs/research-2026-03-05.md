# Realm Codebase Research — 2026-03-05

## Research Summary

| Category              | Findings |
|-----------------------|----------|
| Bugs & TODOs          | 5        |
| Backend Parity        | 5        |
| Convention Violations | 4        |
| Test Gaps             | 5        |
| Feature Gaps          | 5        |
| Architecture          | 5        |

## Recommended Next Steps

1. **[P0/S]** Fix vertex shader return type bug — `engine/src/renderer/opengl/gl_shader.c:141`
2. **[P0/S]** Add GUI ID generation tests (hash collision risk) — `engine/src/gui/gui_id.c`
3. **[P1/S]** Fix unclosed Vulkan loader `dlopen` handle — `engine/src/renderer/vulkan/vk_instance.c:34`
4. **[P1/M]** Add more primitive types (sphere, plane) — `engine/include/renderer/frame_data.h`
5. **[P2/S]** Replace `malloc` with `mem_alloc` in config parser — `engine/src/core/config.c:274`

## Detailed Findings

### Bugs & TODOs
| # | Pri | Effort | File | Description |
|---|-----|--------|------|-------------|
| 1 | P0  | S | `gl_shader.c:141` | `opengl_compile_vertex_shader()` returns `vertex_shader` (u32 GL handle) instead of `true`. Works accidentally since GL IDs are nonzero, but ID 256+ would overflow `b8`. Fragment shader version is correct. |
| 2 | P1  | L | `renderer_frontend.c:156` | `submit_gui_data = nullptr` for Vulkan — GUI is completely non-functional on Vulkan backend |
| 3 | P2  | M | `vk_pipeline.c:86` | TODO: depth/stencil testing disabled (`pDepthStencilState = nullptr`), causes Z-fighting in 3D |
| 4 | P2  | M | `gl_gui.c:328` | TODO: Clay image render command draws gray placeholder rect instead of textured quad |
| 5 | P3  | M | `gl_shader.c:14` | TODO: shader compilation cache for reuse across reloads |

### Backend Parity
| # | Pri | Effort | File | Description |
|---|-----|--------|------|-------------|
| 1 | P1  | L | `renderer_frontend.c:156` | No Vulkan GUI renderer — `vk_gui.c` doesn't exist, no pipeline init |
| 2 | P1  | M | `renderer_frontend.c:71,113,119` | Only 3 of 13 interface function pointers have null safety checks |
| 3 | P2  | S | `vk_text.c:149` | `vulkan_render_text()` is a no-op stub (text is batched differently) |
| 4 | P2  | S | `renderer_frontend.c:149-150` | Dead commented-out function assignments (`//&vulkan_render_text;`) |
| 5 | P2  | L | (structural) | No `vk_gui.c`, no `vk_mesh.c` — OpenGL has dedicated files for both |

### Convention Violations
| # | Pri | Effort | File | Description |
|---|-----|--------|------|-------------|
| 1 | P2  | S | `config.c:274` | Uses raw `malloc()` instead of `mem_alloc()` or scratch arena |
| 2 | P2  | S | `config.c:117` | `static char current_table[32]` — hidden mutable global, should be parsing context |
| 3 | P2  | S | `assert.c:9` | Uses `printf()` instead of `RL_ERROR`/`RL_FATAL` |
| 4 | P2  | S | `platform_linux.c:695` | Uses `fprintf()` for log output instead of logging macros |

### Test Gaps
| # | Pri | Effort | File | Description |
|---|-----|--------|------|-------------|
| 1 | P0  | S | `gui_id.c` | GUI ID hash generation untested — collision risk causes widget state corruption |
| 2 | P1  | M | `gui_text_input.c` | Text input cursor/buffer logic untested — backspace, bounds, caret insertion |
| 3 | P1  | M | `asset.c` | Asset system untested — hash correctness, path handling, lookup |
| 4 | P1  | M | `input.c` | Input state machine transitions (press→hold→release) not covered by existing tests |
| 5 | P2  | L | `profiler.c` | Profiler aggregation, shared memory protocol, lifetime accumulators untested |

### Feature Gaps
| # | Pri | Effort | File | Description |
|---|-----|--------|------|-------------|
| 1 | P1  | M | `frame_data.h` | Only `RL_FRAME_PRIMITIVE_CUBE` exists — no sphere, plane, cylinder, etc. |
| 2 | P1  | L | `asset.h` | No mesh/model asset type — no OBJ/glTF loading, all geometry is hardcoded |
| 3 | P2  | L | (structural) | No scene graph or entity system — objects managed ad-hoc per frame |
| 4 | P3  | L | (missing) | No audio subsystem — zero references to sound/audio anywhere |
| 5 | P3  | L | (missing) | No physics subsystem — no collision, rigid body, or raycasting |

### Architecture
| # | Pri | Effort | File | Description |
|---|-----|--------|------|-------------|
| 1 | P1  | S | `vk_instance.c:34` | `dlopen()` handle never closed — resource leak on macOS Vulkan teardown |
| 2 | P2  | S | `input.h:151` | `input_system_init()` has no matching `input_system_shutdown()` |
| 3 | P2  | M | `vk_instance.c` | Extensive `#if defined(PLATFORM_MACOS)` in renderer core — should be in platform layer |
| 4 | P2  | M | `vk_device.c:280` | Platform-specific Vulkan API version constraint inline instead of abstracted |
| 5 | P3  | L | `profiler.c` | Platform ifdefs throughout for shared memory — could extract to platform layer |
